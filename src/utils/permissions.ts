import { Message, GuildMember, PermissionsBitField } from "discord.js";import prisma from "./prisma";export enum PermissionLevel {    MEMBER = 0,    ADMIN = 1,    CASINO_ADMIN = 2,    OWNER = 3,    BOT_OWNER = 4}const BOT_OWNER_ID = "1288340046449086567";export async function getPermissionLevel(message: Message, member: GuildMember): Promise<PermissionLevel> {    if (member.id === BOT_OWNER_ID) return PermissionLevel.BOT_OWNER;    if (member.id === message.guild!.ownerId) return PermissionLevel.OWNER;    const user = await prisma.user.findUnique({        where: {            discordId_guildId: {                discordId: member.id,                guildId: message.guild!.id            }        }    });    if (user && (user as any).isCasinoAdmin) return PermissionLevel.CASINO_ADMIN;    if (member.permissions.has(PermissionsBitField.Flags.Administrator)) return PermissionLevel.ADMIN;    return PermissionLevel.MEMBER;}export async function canActOn(actorLevel: PermissionLevel, targetLevel: PermissionLevel): Promise<boolean> {    if (actorLevel === PermissionLevel.OWNER) return true;    return actorLevel > targetLevel;}